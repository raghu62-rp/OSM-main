# Backend Environment Variables for Render Deployment

# MongoDB Connection String
# Get this from MongoDB Atlas after creating cluster
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/osm_db?retryWrites=true&w=majority

# JWT Secret for Authentication
# Use a long random string for production
JWT_SECRET=your_super_secure_random_jwt_secret_key_change_this_in_production

# Server Port
PORT=5000

# Node Environment
NODE_ENV=production

# Frontend URL (for CORS)
# Add this after deploying frontend
FRONTEND_URL=https://your-frontend.onrender.com
